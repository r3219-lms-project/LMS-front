# 📚 LMS App
Простое и современное веб-приложение для системы управления обучением (LMS), созданное на **Next.js**  
(это всё ещё React, но с улучшениями для производительности и удобства разработки).

---

## 🚀 Технологии
- [Next.js](https://nextjs.org/) — фронтенд-фреймворк на основе React  
- [Node.js](https://nodejs.org/) — среда выполнения JavaScript  

---

## ⚙️ Установка и запуск

1. **Установите Node.js**  
   Приложение создавалось на версии **v20.19.5**.  
   > ⚠️ Если установить другую версию, приложение может не запуститься из-за несовместимости.

2. **Установите зависимости**  
   ```bash
   npm install

3. **Добавьте файл окружения `.env.local`**
   Создайте в корне проекта файл `.env.local` со следующей переменной (пример для локальной разработки):

   ```
   NEXT_PUBLIC_COURSES_API=http://localhost:8081
   ```

   > ⚠️ Без этого переменные среды, используемые в коде (например
   > `const BASE = process.env.NEXT_PUBLIC_COURSES_API!;`), будут `undefined`,
   > и запросы к API не будут работать.

4. **Запустите приложение в режиме разработки**

   ```bash
   npm run dev
   ```

5. Откройте [http://localhost:3000](http://localhost:3000) в браузере.

---

## 🗂 Структура проекта

* Основная логика и компоненты приложения находятся в папке **`src/`**.
* Маршрутизация и рендеринг управляются самим Next.js.

---

## ⚙️ Настройка backend-сервисов (CORS)

Для корректной работы некоторых сервисов может потребоваться создать конфигурационный файл в вашем Spring Boot-сервисе.
Файл должен находиться по пути (пример для `courseService` — замените название сервиса на своё):

```
src/main/java/com/lms/<ИмяВашегоСервиса>/config/CorsConfig.java
```

Пример содержимого:

```java
package com.lms.courseService.config;

import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.CorsRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

@Configuration
public class CorsConfig implements WebMvcConfigurer {
    @Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/api/**")
            .allowedOrigins("http://localhost:3000")
            .allowedMethods("GET","POST","PUT","PATCH","DELETE","OPTIONS")
            .allowedHeaders("*")
            .allowCredentials(true);
    }
}
```

Если не добавить такую конфигурацию, при работе фронтенда можно получить ошибку:

```
Cross-Origin Request Blocked: The Same Origin Policy disallows reading the remote resource at http://localhost:8081/api/v1/courses. 
(Reason: CORS header ‘Access-Control-Allow-Origin’ missing). Status code: 200
```

Приложение уже содержит пример страницы и готовое подключение к API, чтобы вы могли протестировать работу.
Фронтенд настроен для связи с сервисом **`courseService`**, поэтому необходимо запустить ваш Spring Boot-сервис параллельно.

---

## 💻 Рекомендации по разработке

* Для удобства можно использовать **IntelliJ IDEA** — она отлично поддерживает проекты на React/Next.js (есть кнопка **Run** для быстрого запуска).
